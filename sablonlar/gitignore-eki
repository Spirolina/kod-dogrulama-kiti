# --- Doğrulama katmanı (hedef reponun .gitignore'una eklenir) ---
#
# Varsayılan: dogrulama/ altındaki her şey commit'lenir.
# Task tabanlı ortamda çıktıyı görmenin tek yolu commit; denetim izi de bunu ister.

# Kavrayış sınavı cevap anahtarı — ASLA commit'lenmez.
# Sınavdan ÖNCE üretilir; repoda dururken sınavın anlamı kalmaz.
dogrulama/*/ic/sinav-anahtari.md

# Otomasyon ortam profili — port, auth yöntemi ve hesap anahtarları.
# Kimlik bilgisi $ENV: ile ortam değişkenine işaret etse de bu dosya repoya girmez.
ortam-profili.local.json

# Kimlik bilgilerinin yazıldığı tek yer.
.env.local

# Playwright oturum durumu — CANLI JETON taşır, elle düzenlenmez.
auth/

# Playwright koşum çıktıları — ekran görüntüsü, trace, HTML rapor.
# İçlerinde bakiye, müşteri bilgisi ve ekran içeriği var; kimlik bilgisi kadar hassas.
test-results/
playwright-report/
